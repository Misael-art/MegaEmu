#include <stdint.h>typedef struct {    NodeID id;    NodeStatus status;    MetricCollection metrics;    NetworkInfo network;} MonitoringNode;typedef struct {    MonitoringNode** nodes;    size_t node_count;    SyncStrategy sync_strategy;        void (*register_node)(NodeInfo* info);    void (*collect_metrics)(NodeID id);    void (*sync_nodes)(void);    void (*handle_node_failure)(NodeID id);    NodeReport* (*generate_node_report)(NodeID id);} DistributedMonitor;DistributedMonitor* distributed_monitor_create(const MonitorConfig* config);