#include <stdint.h>typedef struct {    double threshold;    AlertSeverity severity;    bool (*condition)(const EventMetrics* metrics);    void (*callback)(const Alert* alert);} AlertRule;typedef struct {    AlertRule* rules;    size_t rule_count;    AlertHistory history;        void (*add_rule)(AlertRule rule);    void (*check_metrics)(const EventMetrics* metrics);    void (*notify)(const Alert* alert);} AlertSystem;AlertSystem* alert_system_create(void);void alert_system_process(AlertSystem* system, const EventMetrics* metrics);