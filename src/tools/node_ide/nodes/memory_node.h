#include <stdint.h>#ifndef EMU_MEMORY_NODE_H#define EMU_MEMORY_NODE_Htypedef struct {    uint32_t address;      // Endereço base    uint16_t size;         // Tamanho em bytes    uint8_t access_mode;   // READ/WRITE/BOTH    bool volatile_mem;     // Memória volátil?} EmuMemoryNodeParams;emu_status_t emu_memory_node_init(EmuNode* node);emu_status_t emu_memory_node_process(EmuNode* node);#endif // EMU_MEMORY_NODE_H