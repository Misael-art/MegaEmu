#include <stdint.h>#ifndef EMU_AUDIO_NODE_H#define EMU_AUDIO_NODE_Htypedef struct {    uint16_t frequency;    // Hz    uint8_t volume;        // 0-255    uint8_t channel;       // Canal de áudio    uint16_t duration_ms;  // Duração em ms} EmuAudioNodeParams;emu_status_t emu_audio_node_init(EmuNode* node);emu_status_t emu_audio_node_process(EmuNode* node);#endif // EMU_AUDIO_NODE_H