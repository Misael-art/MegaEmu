#ifndef EMU_NODE_VALIDATOR_H#define EMU_NODE_VALIDATOR_H#include <stdint.h>#include <stdbool.h>#include "../core/node_types.h"#include "../core/node_enums.h"#include "../../utils/common_types.h"/** * @brief Estrutura para armazenar informações de erro de validação */typedef struct{    emu_validation_code_t error_code;    char message[256];} EmuValidationError;/** * @brief Estrutura para validadores de nós */typedef struct{    bool (*validate)(EmuNode *node, EmuValidationError *error);    const char *type_name;} EmuNodeValidator;// Usando emu_error_t em vez de emu_status_temu_error_t emu_register_node_validator(emu_node_type_t type, EmuNodeValidator validator);emu_error_t emu_validate_node(EmuNode *node, EmuValidationError *error);#endif // EMU_NODE_VALIDATOR_H