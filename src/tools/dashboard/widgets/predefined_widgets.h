#include <stdint.h>typedef struct {    Widget base;    MetricsGraph* graph;    ColorScheme colors;    DataRange range;} PerformanceGraphWidget;typedef struct {    Widget base;    AlertList* alerts;    FilterConfig filters;} AlertWidget;typedef struct {    Widget base;    SystemStatus* status;    RefreshRate refresh_rate;} SystemMonitorWidget;PerformanceGraphWidget* performance_graph_widget_create(void);AlertWidget* alert_widget_create(void);SystemMonitorWidget* system_monitor_widget_create(void);