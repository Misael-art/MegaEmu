#include <stdint.h>typedef struct {    const char* widget_id;    WidgetType type;    Vector2D position;    Vector2D size;    void* data;    void (*render)(const Widget* widget);    void (*update)(Widget* widget, const void* data);} Widget;typedef struct {    Widget** widgets;    size_t widget_count;    Layout layout;    Theme theme;        void (*add_widget)(const Widget* widget);    void (*remove_widget)(const char* widget_id);    void (*save_layout)(const char* filename);    void (*load_layout)(const char* filename);    void (*update_all)(const MetricsData* data);} Dashboard;Dashboard* dashboard_create(void);void dashboard_destroy(Dashboard* dash);