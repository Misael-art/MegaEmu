# CMakeLists.txt para o monitor de áudio# Coletar arquivos fontefile(GLOB SOUND_MONITOR_SOURCES "*.c")file(GLOB SOUND_MONITOR_HEADERS "*.h")# Definir a biblioteca para o monitor de áudioadd_library(emu_tools_sound_monitor STATIC    ${SOUND_MONITOR_SOURCES}    ${SOUND_MONITOR_HEADERS})# Dependênciastarget_link_libraries(emu_tools_sound_monitor    emu_core    emu_tools_core)# Exportar headers para outros componentestarget_include_directories(emu_tools_sound_monitor PUBLIC    ${CMAKE_CURRENT_SOURCE_DIR})# Instalar biblioteca e headersinstall(TARGETS emu_tools_sound_monitor DESTINATION lib)install(FILES ${SOUND_MONITOR_HEADERS} DESTINATION include/tools/sound_monitor) 