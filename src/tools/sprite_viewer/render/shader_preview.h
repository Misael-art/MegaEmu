#include <stdint.h>typedef struct {    RenderTexture2D target;    Rectangle viewport;    float update_rate;    bool auto_refresh;        struct {        Shader* current;        ShaderUniform* uniforms;        size_t uniform_count;    } shader_data;        struct {        void (*on_update)(float delta);        void (*on_uniform_change)(const char* name);        void (*on_compile_error)(const char* error);    } callbacks;} ShaderPreview;void shader_preview_init(void);void shader_preview_update(float delta);void shader_preview_render(void);bool shader_preview_hot_reload(const char* source);void shader_preview_set_uniform(const char* name, const void* value);