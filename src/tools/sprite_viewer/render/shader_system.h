#include <stdint.h>typedef struct {    const char* name;    ShaderType type;    const char* source_code;    ShaderUniformList uniforms;    PlatformCompatibility platform_support;} CustomShader;void shader_system_init(void);ShaderHandle shader_load_custom(const char* path);void shader_bind_uniforms(ShaderHandle handle, const UniformData* data);bool shader_validate_platform(ShaderHandle handle, PlatformType platform);