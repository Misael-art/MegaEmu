#include <stdint.h>#ifndef CUSTOM_PALETTE_H#define CUSTOM_PALETTE_H#include "core/color.h"typedef struct {    char* name;    Color* colors;    uint32_t color_count;    PlatformType target_platform;} CustomPalette;CustomPalette* pal_create(const char* name, uint32_t size);void pal_import_from_image(CustomPalette* pal, const char* image_path);void pal_optimize_for_platform(CustomPalette* pal, PlatformType platform);void pal_export(CustomPalette* pal, const char* output_path);bool pal_validate_constraints(CustomPalette* pal, PlatformType platform);#endif