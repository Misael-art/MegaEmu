#include <stdint.h>typedef struct {    uint32_t frame_index;    uint32_t duration;    Rectangle bounds;    float similarity_score;} AnimationFrame;typedef struct {    AnimationFrame* frames;    uint32_t frame_count;    float fps;    bool is_looping;    char name[32];} Animation;typedef struct {    Animation* animations;    size_t animation_count;        struct {        float similarity_threshold;        uint32_t min_frames;        uint32_t max_frames;        float min_fps;        float max_fps;    } config;} AnimationDetector;Animation* detect_animations(const SpriteSheet* sheet);void optimize_animation(Animation* anim);bool export_animation(const Animation* anim, ExportFormat format);