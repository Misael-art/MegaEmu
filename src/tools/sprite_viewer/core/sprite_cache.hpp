#include <stdint.h>#pragma once#include <unordered_map>#include <string>#include <vector>#include <memory>#include "logger.hpp"class SpriteCache {public:    explicit SpriteCache(size_t maxSize = 1000);        std::shared_ptr<std::vector<uint8_t>> get(const std::string& key);    void put(const std::string& key, std::vector<uint8_t> spriteData);    private:    size_t maxSize;    std::unordered_map<std::string, std::shared_ptr<std::vector<uint8_t>>> cache;    Logger logger;};