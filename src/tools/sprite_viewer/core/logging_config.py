import loggingimport osfrom datetime import datetimeclass SpriteViewerLogger:    @staticmethod    def setup():        """Configura sistema de logging"""        log_dir = "logs/sprite_viewer"        os.makedirs(log_dir, exist_ok=True)                log_file = f"{log_dir}/sprite_viewer_{datetime.now():%Y%m%d}.log"                logging.basicConfig(            level=logging.INFO,            format='%(asctime)s [%(levelname)s] %(message)s',            handlers=[                logging.FileHandler(log_file),                logging.StreamHandler()            ]        )                logging.info("Sprite Viewer logging initialized")    @staticmethod    def log_sprite_operation(operation, details):        """Registra operação com sprites"""        logging.info(f"Sprite operation: {operation} - {details}")