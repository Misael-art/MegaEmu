#include <stdint.h>#ifndef SPRITE_COMPRESSION_H#define SPRITE_COMPRESSION_Htypedef enum {    COMPRESS_RLE,    COMPRESS_HUFFMAN,    COMPRESS_LZ77,    COMPRESS_PLATFORM_SPECIFIC} CompressionType;typedef struct {    CompressionType type;    uint32_t quality_level;    bool preserve_palette;    PlatformType target_platform;} CompressionSettings;void compress_init_system(void);uint32_t compress_sprite(Sprite* sprite, CompressionSettings* settings);void compress_export(const char* output_path, CompressionType type);float compress_calculate_ratio(Sprite* sprite, CompressionSettings* settings);#endif