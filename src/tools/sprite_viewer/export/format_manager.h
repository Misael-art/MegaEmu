#include <stdint.h>typedef struct {    const char* format_name;    const char* extension;    uint32_t version;        struct {        bool supports_compression;        bool supports_metadata;        bool supports_layers;        size_t max_dimensions;    } capabilities;        struct {        void (*encode)(const AssetData* data, ExportOptions* options);        void (*decode)(const char* input, AssetData* output);        bool (*validate)(const AssetData* data);    } handlers;} ExportFormat;void format_manager_init(void);void format_register(ExportFormat* format);ExportFormat* format_get_by_extension(const char* ext);bool format_validate_compatibility(const AssetData* data, const char* format);