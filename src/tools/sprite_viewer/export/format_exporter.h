#include <stdint.h>typedef struct {    const char* format_name;    uint32_t version;    ExportSettings settings;        void (*export_func)(const Asset* asset, const char* path);    bool (*validate_func)(const Asset* asset);    void (*optimize_func)(Asset* asset, PlatformType platform);} FormatExporter;void exporter_register_format(FormatExporter* exporter);bool exporter_validate_asset(const Asset* asset, const char* format);void exporter_save_asset(const Asset* asset, const char* path, const char* format);