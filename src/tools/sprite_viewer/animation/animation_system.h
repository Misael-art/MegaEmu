#include <stdint.h>#ifndef ANIMATION_SYSTEM_H#define ANIMATION_SYSTEM_H#include "core/types.h"typedef struct {    uint32_t frame_index;    uint32_t duration_ms;    Rectangle frame_rect;    bool is_keyframe;} AnimationFrame;typedef struct {    char* name;    AnimationFrame* frames;    uint32_t frame_count;    bool loop;    float speed_multiplier;} Animation;void anim_init_system(void);Animation* anim_create(const char* name);void anim_add_frame(Animation* anim, Rectangle frame_rect, uint32_t duration);void anim_play(Animation* anim);void anim_stop(Animation* anim);void anim_update(float delta_time);#endif