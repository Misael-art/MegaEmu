#include <stdint.h>#ifndef FRAME_INTERPOLATION_H#define FRAME_INTERPOLATION_Htypedef enum {    INTERP_LINEAR,    INTERP_SMOOTH,    INTERP_EASE_IN,    INTERP_EASE_OUT} InterpolationType;typedef struct {    InterpolationType type;    float strength;    bool enabled;} InterpolationSettings;void interp_init_system(void);void interp_calculate_frame(Animation* anim, float time, InterpolationSettings* settings);void interp_apply_settings(Animation* anim, InterpolationSettings* settings);bool interp_validate_settings(InterpolationSettings* settings);#endif