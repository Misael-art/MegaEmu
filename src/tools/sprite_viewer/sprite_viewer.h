#include <stdint.h>#ifndef SPRITE_VIEWER_H#define SPRITE_VIEWER_H#include "core/types.h"#include "core/platform.h"typedef struct {    uint32_t width;    uint32_t height;    uint8_t* data;    uint32_t* palette;    PlatformType target_platform;} Sprite;typedef struct {    Sprite* sprites;    size_t sprite_count;    char* metadata;    bool animation_active;    uint32_t current_frame;} SpriteSheet;// Core functionalitySpriteSheet* sv_load_spritesheet(const char* filename);void sv_export_sprite(Sprite* sprite, const char* filename, ExportFormat format);void sv_convert_platform(Sprite* sprite, PlatformType target);void sv_generate_datasheet(SpriteSheet* sheet, const char* output_path);// Real-time previewvoid sv_start_emulator_preview(Sprite* sprite);void sv_update_preview(void);void sv_stop_preview(void);#endif // SPRITE_VIEWER_H