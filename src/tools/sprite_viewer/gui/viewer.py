import tkinter as tkfrom tkinter import ttkimport numpy as npfrom PIL import Image, ImageTkfrom ..core.logging_config import SpriteViewerLoggerfrom ..core.cache import SpriteCacheclass SpriteViewer(tk.Frame):    def __init__(self, master=None):        super().__init__(master)        self.logger = SpriteViewerLogger()        self.cache = SpriteCache()        self.init_ui()    def init_ui(self):        """Inicializa interface do usuário"""        self.master.title("Mega_Emu Sprite Viewer")                # Painel de controles        self.control_frame = ttk.LabelFrame(self, text="Controles")        self.control_frame.pack(fill="x", padx=5, pady=5)                # Seletor de plataforma        ttk.Label(self.control_frame, text="Plataforma:").pack(side="left")        self.platform_var = tk.StringVar(value="SNES")        self.platform_combo = ttk.Combobox(            self.control_frame,             textvariable=self.platform_var,            values=["SNES", "Mega Drive"]        )        self.platform_combo.pack(side="left", padx=5)                # Canvas para visualização        self.canvas = tk.Canvas(self, width=512, height=512)        self.canvas.pack(fill="both", expand=True)                self.logger.log_sprite_operation("GUI", "Interface initialized")