#include <stdint.h>typedef struct {    SpriteMetadata** selected_sprites;    size_t selection_count;        struct {        ColorPalette* palette;        int32_t offset_x;        int32_t offset_y;        float scale;        bool flip_h;        bool flip_v;    } batch_params;} BatchEditor;void batch_editor_init(BatchEditor* editor);void batch_editor_apply(BatchEditor* editor);void batch_editor_render_ui(BatchEditor* editor);