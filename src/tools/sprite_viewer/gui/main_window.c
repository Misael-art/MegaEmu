#include "gui/main_window.h"#include "imgui/imgui.h"void render_main_window(void) {    ImGui::Begin("Sprite Viewer");        // Sprite preview panel    ImGui::BeginChild("Preview", ImVec2(640, 480));    render_sprite_preview();    ImGui::EndChild();        // Tools panel    ImGui::BeginChild("Tools", ImVec2(200, 480));    render_tool_panel();    ImGui::EndChild();        // Platform selection    if (ImGui::BeginCombo("Target Platform", current_platform_name)) {        for (int i = 0; i < PLATFORM_COUNT; i++) {            if (ImGui::Selectable(platform_names[i])) {                change_target_platform(i);            }        }        ImGui::EndCombo();    }        ImGui::End();}