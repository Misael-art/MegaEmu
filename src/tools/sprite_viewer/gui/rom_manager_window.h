#include <stdint.h>typedef struct {    bool visible;    ROMExtractor* current_rom;        struct {        Button* load_rom_btn;        ListView* sprite_list;        TabView* platform_tabs;        PreviewPanel* sprite_preview;    } widgets;        struct {        void (*on_sprite_selected)(SpriteMetadata* sprite);        void (*on_export_sprite)(SpriteMetadata* sprite);    } callbacks;} ROMManagerWindow;void rom_manager_init(void);void rom_manager_update(void);void rom_manager_render(void);