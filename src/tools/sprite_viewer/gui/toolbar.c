#include "gui/toolbar.h"#include "imgui/imgui.h"void render_toolbar(void) {    ImGui::BeginMainMenuBar();        if (ImGui::BeginMenu("File")) {        if (ImGui::MenuItem("Import Sprite")) handle_import();        if (ImGui::MenuItem("Export As...")) handle_export();        if (ImGui::MenuItem("Generate Datasheet")) generate_datasheet();        ImGui::EndMenu();    }        if (ImGui::BeginMenu("Edit")) {        if (ImGui::MenuItem("Flip Horizontal")) flip_sprite_h();        if (ImGui::MenuItem("Flip Vertical")) flip_sprite_v();        if (ImGui::MenuItem("Rotate")) rotate_sprite();        ImGui::EndMenu();    }        if (ImGui::BeginMenu("View")) {        ImGui::MenuItem("Show Grid", NULL, &show_grid);        ImGui::MenuItem("Show Collision", NULL, &show_collision);        ImGui::SliderFloat("Zoom", &zoom_level, 1.0f, 8.0f);        ImGui::EndMenu();    }        ImGui::EndMainMenuBar();}