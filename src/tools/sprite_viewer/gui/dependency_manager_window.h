#include <stdint.h>typedef struct {    bool visible;    Vector2 position;    Vector2 size;        struct {        ScrollView* plugin_list;        Button* refresh_btn;        Button* resolve_btn;        Label* status_label;    } widgets;        struct {        void (*on_resolve)(void);        void (*on_plugin_select)(const char* plugin_id);    } callbacks;} DependencyManagerWindow;void dependency_window_init(void);void dependency_window_update(void);void dependency_window_render(void);void dependency_window_refresh_list(void);