#include <stdint.h>typedef struct {    const char* plugin_id;    const char* version;    bool optional;} PluginDependency;typedef struct {    Plugin* plugin;    PluginDependency* dependencies;    size_t dependency_count;    bool is_satisfied;        struct {        void (*on_dependency_load)(const char* plugin_id);        void (*on_dependency_unload)(const char* plugin_id);        void (*on_dependency_error)(const char* plugin_id, const char* error);    } callbacks;} PluginDependencyNode;void dependency_manager_init(void);bool dependency_add(Plugin* plugin, PluginDependency* deps, size_t count);bool dependency_validate(const char* plugin_id);void dependency_resolve_all(void);PluginDependency* dependency_get_missing(const char* plugin_id, size_t* count);