#include <stdint.h>#ifndef REALTIME_PREVIEW_H#define REALTIME_PREVIEW_H#include "core/types.h"#include "animation/animation_system.h"typedef struct {    SDL_Window* window;    SDL_Renderer* renderer;    SDL_Texture* preview_texture;    bool is_playing;    float zoom_level;    Vector2 pan_offset;} PreviewWindow;void preview_init(void);void preview_update(float delta_time);void preview_render_animation(Animation* anim);void preview_set_zoom(float level);void preview_set_pan(Vector2 offset);void preview_toggle_play(void);#endif