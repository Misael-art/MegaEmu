#include <stdint.h>typedef enum {    COMPRESS_NONE,    COMPRESS_ZLIB,    COMPRESS_LZ4,    COMPRESS_ZSTD} CompressionType;typedef struct {    CompressionType type;    uint32_t level;    size_t (*compress)(const void*, size_t, void*, size_t);    size_t (*decompress)(const void*, size_t, void*, size_t);} Compressor;typedef struct {    char magic[4];    uint32_t version;    CompressionType compression;    uint32_t original_size;    uint32_t compressed_size;    uint8_t data[];} CacheHeader;bool cache_compress_data(const void* data, size_t size,                         CompressionType type, const char* output_path);bool cache_decompress_file(const char* cache_path, void** out_data,                           size_t* out_size);