#include <stdint.h>typedef struct {    char rom_hash[64];    PlatformType platform;    uint32_t sprite_count;    time_t last_modified;    char cache_file[256];} ROMCacheEntry;typedef struct {    ROMCacheEntry* entries;    size_t entry_count;    char cache_dir[256];} ROMCache;ROMCache* rom_cache_init(const char* cache_dir);bool rom_cache_store(ROMCache* cache, const ROMExtractor* extractor);bool rom_cache_load(ROMCache* cache, ROMExtractor* extractor);void rom_cache_cleanup(ROMCache* cache);