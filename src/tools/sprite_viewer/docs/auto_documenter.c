#include "docs/auto_documenter.h"#include "core/platform_specs.h"void generate_sprite_documentation(Sprite* sprite, const char* output_path) {    Documentation doc;    doc_init(&doc);        // Adiciona informações técnicas    doc_add_section(&doc, "Technical Specifications");    doc_add_property(&doc, "Dimensions", "%dx%d", sprite->width, sprite->height);    doc_add_property(&doc, "Color Depth", "%d bits", get_color_depth(sprite));        // Adiciona informações de compatibilidade    doc_add_section(&doc, "Platform Compatibility");    for (int i = 0; i < PLATFORM_COUNT; i++) {        PlatformSpecs specs = get_platform_specs(i);        doc_add_compatibility(&doc, platform_names[i],                             check_platform_compatibility(sprite, &specs));    }        // Gera preview    doc_add_section(&doc, "Preview");    generate_preview_image(&doc, sprite);        // Salva documentação    doc_save(&doc, output_path);}