#include "docs/datasheet_generator.h"#include "core/platform_specs.h"void sv_generate_datasheet(SpriteSheet* sheet, const char* output_path) {    FILE* fp = fopen(output_path, "w");        // Generate header    fprintf(fp, "# Sprite Sheet Documentation\n\n");        // Technical specifications    fprintf(fp, "## Technical Specifications\n");    fprintf(fp, "- Format: %s\n", get_format_name(sheet->format));    fprintf(fp, "- Dimensions: %dx%d\n", sheet->width, sheet->height);    fprintf(fp, "- Colors: %d\n", sheet->palette_size);        // Platform compatibility    fprintf(fp, "\n## Platform Compatibility\n");    for (int i = 0; i < PLATFORM_COUNT; i++) {        PlatformSpecs specs = get_platform_specs(i);        bool compatible = check_compatibility(sheet, &specs);        fprintf(fp, "- %s: %s\n", platform_names[i],                 compatible ? "✅ Compatible" : "❌ Incompatible");    }        fclose(fp);}