#include <stdint.h>typedef struct {    Rectangle preview_area;    float zoom_level;    bool show_hitboxes;    bool show_timeline;        struct {        void (*on_frame_selected)(uint32_t frame_idx);        void (*on_timeline_scroll)(float position);        void (*on_preview_zoom)(float level);    } callbacks;} AnimationUI;typedef struct {    const char* name;    uint32_t shortcut_key;    void (*action)(Animation* anim);} AnimationTool;void ui_init_animation_interface(void);void ui_draw_animation_timeline(const Animation* anim);void ui_handle_animation_input(void);