#include <stdint.h>typedef enum {    SCRIPT_LANG_LUA,    SCRIPT_LANG_PYTHON,    SCRIPT_LANG_JAVASCRIPT} ScriptLanguage;typedef struct {    const char* script_path;    ScriptLanguage language;    void* engine_handle;        struct {        bool (*init)(void);        bool (*execute)(const char* function);        void (*cleanup)(void);    } ops;} ScriptEngine;typedef struct {    const char* name;    const char* description;    const char* script_content;    ScriptLanguage language;} AutomationScript;bool script_load_automation(const char* script_path);bool script_execute_task(const char* task_name);void script_register_callback(const char* event,                             void (*callback)(void*));