#include <stdint.h>typedef struct {    const char* name;    uint32_t shortcut_key;    bool enabled;    void (*execute)(AnimationData* data);} AnimationTool;typedef struct {    Vector2 position;    float rotation;    Vector2 scale;    uint32_t frame_duration;} KeyFrame;typedef struct {    const char* name;    KeyFrame* frames;    size_t frame_count;    bool loop;        struct {        void (*on_frame_update)(uint32_t frame_idx);        void (*on_sequence_complete)(void);    } callbacks;} AnimationSequence;// Novas ferramentas de animaçãovoid tool_init_bone_system(void);void tool_init_tweening(void);void tool_init_path_editor(void);void tool_init_frame_interpolator(void);