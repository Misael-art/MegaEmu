#include <stdint.h>typedef struct {    const char* id;    const char* title;    const char* description;    uint32_t difficulty;        struct {        void (*start)(void);        bool (*check_progress)(void);        void (*complete)(void);    } lifecycle;        struct {        const char* content;        Vector2 position;        bool is_interactive;    } steps[MAX_TUTORIAL_STEPS];} Tutorial;// Sistema de tutoriaisvoid tutorial_system_init(void);void tutorial_register(const Tutorial* tutorial);void tutorial_start(const char* tutorial_id);void tutorial_update(float delta_time);