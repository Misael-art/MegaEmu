#include <stdint.h>typedef enum {    EXPORT_FORMAT_SPINE,    // Spine Animation    EXPORT_FORMAT_GODOT,    // Godot Engine    EXPORT_FORMAT_UNREAL,   // Unreal Engine    EXPORT_FORMAT_CONSTRUCT // Construct Engine} ExportFormatExtended;typedef struct {    const char* format_name;    const char* file_extension;    bool supports_animation;    bool supports_metadata;    ExportOptions options;} FormatDescriptor;bool export_to_format(const SpriteData* data,                      ExportFormatExtended format,                     const char* output_path);