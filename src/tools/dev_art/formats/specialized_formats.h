#include <stdint.h>typedef struct {    const char* format_name;    uint32_t version;        struct {        bool supports_layers;        bool supports_animation;        bool supports_metadata;        bool supports_compression;    } capabilities;        struct {        void* (*load)(const char* path);        bool (*save)(const void* data, const char* path);        void (*free)(void* data);    } handlers;} SpecializedFormat;// Novos formatos especializadosvoid register_blender_format(void);void register_maya_format(void);void register_unity_format(void);void register_houdini_format(void);