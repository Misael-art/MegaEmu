#include <stdint.h>typedef enum {    FORMAT_ASEPRITE,    FORMAT_PIXELORAMA,    FORMAT_PYXEL_EDIT,    FORMAT_GRAPHICS_GALE} ExtendedFormats;typedef struct {    const char* format_name;    const char* extensions[4];    uint32_t capabilities;        struct {        bool (*can_import)(const char* file_path);        bool (*can_export)(const SpriteData* data);    } validation;        struct {        SpriteData* (*import)(const char* file_path);        bool (*export)(const SpriteData* data, const char* file_path);    } operations;} FormatHandler;void register_format_handler(const FormatHandler* handler);bool validate_format_support(const char* file_path);