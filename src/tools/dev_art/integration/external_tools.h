#include <stdint.h>typedef struct {    const char* tool_name;    const char* executable_path;    bool (*initialize)(void);    void (*cleanup)(void);    bool (*export_data)(const void*, size_t);    bool (*import_data)(void**, size_t*);} ExternalTool;typedef struct {    ExternalTool* tools;    size_t tool_count;        struct {        void (*on_export_complete)(const char* tool, bool success);        void (*on_import_complete)(const char* tool, bool success);    } callbacks;} ToolManager;// Implementações específicasstatic const ExternalTool TOOL_IMPLEMENTATIONS[] = {    {        .tool_name = "Aseprite",        .initialize = aseprite_init,        .export_data = aseprite_export,        .import_data = aseprite_import    },    {        .tool_name = "Tiled",        .initialize = tiled_init,        .export_data = tiled_export,        .import_data = tiled_import    }};