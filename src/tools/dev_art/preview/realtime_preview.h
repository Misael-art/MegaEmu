#include <stdint.h>typedef struct {    SDL_Window* window;    SDL_Renderer* renderer;    SDL_Texture* preview_texture;        struct {        uint32_t width;        uint32_t height;        float scale;        bool vsync;    } display;        struct {        bool show_grid;        bool show_bounds;        bool show_origin;        Vector4 background_color;    } options;} PreviewContext;void preview_init(void);void preview_update(float delta_time);void preview_render_sprite(const SpriteData* sprite);void preview_handle_input(void);