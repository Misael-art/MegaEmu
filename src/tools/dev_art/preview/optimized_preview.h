#include <stdint.h>typedef struct {    uint32_t max_sprites;    uint32_t texture_pool_size;    bool use_hardware_acceleration;    bool enable_multithreading;} PreviewOptimizationConfig;typedef struct {    void* sprite_cache;    ThreadPool* render_threads;    GPUBuffer* vertex_buffer;    ShaderProgram* shader;} PreviewOptimizationContext;// Sistema de otimizaçãovoid preview_optimize_init(const PreviewOptimizationConfig* config);void preview_optimize_batch_render(const SpriteData** sprites, size_t count);void preview_optimize_cache_sprite(const SpriteData* sprite);