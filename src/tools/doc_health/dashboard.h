#include <stdint.h>typedef struct {    struct {        int32_t total_files;        int32_t broken_links;        int32_t missing_images;        float test_coverage;        time_t last_update;    } metrics;    struct {        char** broken_links;        char** outdated_docs;        char** missing_sections;    } issues;    void (*update_metrics)(void);    void (*generate_report)(const char* output_path);    void (*export_dashboard)(const char* format);} DocHealthDashboard;DocHealthDashboard* doc_health_dashboard_create(void);void doc_health_dashboard_destroy(DocHealthDashboard* dashboard);