#include <stdint.h>typedef struct {    struct {        float readability_score;        float completeness_score;        float freshness_score;        float consistency_score;    } quality_metrics;    struct {        size_t words_count;        size_t code_blocks;        size_t images;        size_t internal_links;        size_t external_links;    } content_metrics;    void (*collect_metrics)(const char* doc_path);    void (*analyze_quality)(void);    void (*export_metrics)(const char* output_path);} DocMetricsCollector;