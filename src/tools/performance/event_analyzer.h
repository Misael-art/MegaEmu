#include <stdint.h>typedef struct {    uint64_t timestamp;    uint32_t event_type;    uint32_t processing_time;    uint32_t queue_size;} EventMetrics;typedef struct {    EventMetrics* metrics_buffer;    size_t buffer_size;    size_t current_index;    bool recording;        void (*start_recording)(void);    void (*stop_recording)(void);    void (*export_metrics)(const char* filename);    void (*analyze_hotspots)(void);} EventAnalyzer;EventAnalyzer* event_analyzer_create(size_t buffer_size);void event_analyzer_destroy(EventAnalyzer* analyzer);