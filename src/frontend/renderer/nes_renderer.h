#ifndef NES_RENDERER_H#define NES_RENDERER_H#include <SDL2/SDL.h>#include <stdbool.h>#include <stdint.h>#include "../../platforms/nes/nes_config.h"#ifdef __cplusplusextern "C"{#endif    typedef struct    {        SDL_Window *window;        SDL_Renderer *renderer;        SDL_Texture *game_texture;        int window_width;        int window_height;        int scale_factor;        bool vsync_enabled;        bool fullscreen;    } nes_renderer_t;    /**     * @brief Inicializa o renderer do NES     * @param window Janela SDL     * @param renderer Renderer SDL     * @return 0 em caso de sucesso, código de erro caso contrário     */    int32_t nes_renderer_init(SDL_Window *window, SDL_Renderer *renderer);    /**     * @brief Finaliza o renderer do NES     */    void nes_renderer_shutdown(void);    /**     * @brief Renderiza um frame do NES     * @param frame_buffer Buffer contendo os pixels do frame     * @return 0 em caso de sucesso, código de erro caso contrário     */    int32_t nes_renderer_render_frame(const uint32_t *frame_buffer);    /**     * @brief Atualiza o tamanho da janela     * @param width Nova largura     * @param height Nova altura     */    void nes_renderer_resize(int width, int height);    /**     * @brief Define o modo de tela cheia     * @param fullscreen true para tela cheia, false para janela     */    void nes_renderer_set_fullscreen(bool fullscreen);    /**     * @brief Define o fator de escala     * @param scale Fator de escala (1-4)     */    void nes_renderer_set_scale(int scale);    /**     * @brief Habilita/desabilita vsync     * @param enabled true para habilitar, false para desabilitar     */    void nes_renderer_set_vsync(bool enabled);#ifdef __cplusplus}#endif#endif /* NES_RENDERER_H */