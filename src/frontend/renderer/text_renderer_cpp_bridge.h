/** * @file text_renderer_cpp_bridge.h * @brief Declarações da ponte entre C++ e C para o sistema de renderização de texto */#ifndef TEXT_RENDERER_CPP_BRIDGE_H#define TEXT_RENDERER_CPP_BRIDGE_H#include "text_renderer.h"#ifdef __cplusplusextern "C"{#endif    /**     * @brief Renderiza texto na tela com estilo personalizado     *     * @param text Texto a ser renderizado     * @param x Posição X     * @param y Posição Y     * @param color Cor do texto     * @param size Tamanho do texto     * @return true se a renderização foi bem-sucedida, false caso contrário     */    bool text_renderer_draw(const char *text, int x, int y, SDL_Color color, text_size_t size);    /**     * @brief Renderiza texto na tela com alinhamento     *     * @param text Texto a ser renderizado     * @param x Posição X     * @param y Posição Y     * @param color Cor do texto     * @param size Tamanho do texto     * @param align Alinhamento do texto     * @return true se a renderização foi bem-sucedida, false caso contrário     */    bool text_renderer_draw_aligned(const char *text, int x, int y, SDL_Color color, text_size_t size, text_align_t align);    /**     * @brief Renderiza texto na tela com estilo completo     *     * @param text Texto a ser renderizado     * @param x Posição X     * @param y Posição Y     * @param color Cor do texto     * @param size Tamanho do texto     * @param align Alinhamento do texto     * @param style_flags Flags de estilo do texto     * @return true se a renderização foi bem-sucedida, false caso contrário     */    bool text_renderer_draw_styled(const char *text, int x, int y, SDL_Color color, text_size_t size, text_align_t align, text_style_flags_t style_flags);    /**     * @brief Mede o tamanho que um texto ocuparia se renderizado     *     * @param text Texto a ser medido     * @param size Tamanho do texto     * @param width Ponteiro para armazenar a largura     * @param height Ponteiro para armazenar a altura     * @return true se a medição foi bem-sucedida, false caso contrário     */    bool text_renderer_measure_size(const char *text, text_size_t size, int *width, int *height);#ifdef __cplusplus}#endif#endif /* TEXT_RENDERER_CPP_BRIDGE_H */