// ✅ OTIMIZADO: Separação em dois passes (horizontal/vertical)void main() {    // ... código existente ...    // Usa separabilidade da Gaussiana para reduzir para O(2n)    vec2 direction = horizontal ? vec2(1.0, 0.0) : vec2(0.0, 1.0);    // Amostragem linear para reduzir operações de textura    vec3 result = vec3(0.0);    for (int i = -radius; i <= radius; i++) {        float weight = gaussianWeight(float(i), sigma);        vec2 offset = direction * float(i) / textureSize;        result += texture(inputTexture, texCoord + offset).rgb * weight;    }    // ... código existente ...}