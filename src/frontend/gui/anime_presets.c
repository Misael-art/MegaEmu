#include "anime_effects.h"// Preset moderno (estilo Demon Slayer)anime_preset_t anime_preset_modern(void){    anime_preset_t preset = {0};    preset.name = "Modern Anime";    preset.style = ANIME_STYLE_MODERN;    // Configurações de contorno    preset.params.outline.thickness = 2.0f;    preset.params.outline.edge_threshold = 0.3f;    preset.params.outline.color = (gui_color_t){0, 0, 0, 255};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_TRUE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 4;    preset.params.cel.smoothness = 0.1f;    preset.params.cel.specular = GUI_TRUE;    preset.params.cel.shade_threshold[0] = 0.2f;    preset.params.cel.shade_threshold[1] = 0.4f;    preset.params.cel.shade_threshold[2] = 0.6f;    preset.params.cel.shade_threshold[3] = 0.8f;    // Configurações de cor    preset.params.color.color_levels = 12;    preset.params.color.saturation = 1.3f;    preset.params.color.contrast = 1.2f;    preset.params.color.posterize = GUI_TRUE;    // Configurações de bloom    preset.params.bloom.intensity = 0.8f;    preset.params.bloom.threshold = 0.7f;    preset.params.bloom.radius = 10.0f;    preset.params.bloom.tint = (gui_color_t){255, 240, 200, 255};    preset.params.bloom.anamorphic = GUI_TRUE;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_TRUE;    return preset;}// Preset clássico (estilo Dragon Ball)anime_preset_t anime_preset_classic(void){    anime_preset_t preset = {0};    preset.name = "Classic Anime";    preset.style = ANIME_STYLE_CLASSIC;    // Configurações de contorno    preset.params.outline.thickness = 1.5f;    preset.params.outline.edge_threshold = 0.4f;    preset.params.outline.color = (gui_color_t){0, 0, 0, 255};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_FALSE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 3;    preset.params.cel.smoothness = 0.2f;    preset.params.cel.specular = GUI_FALSE;    preset.params.cel.shade_threshold[0] = 0.3f;    preset.params.cel.shade_threshold[1] = 0.6f;    preset.params.cel.shade_threshold[2] = 0.9f;    // Configurações de cor    preset.params.color.color_levels = 8;    preset.params.color.saturation = 1.1f;    preset.params.color.contrast = 1.1f;    preset.params.color.posterize = GUI_TRUE;    // Configurações de bloom    preset.params.bloom.intensity = 0.5f;    preset.params.bloom.threshold = 0.8f;    preset.params.bloom.radius = 5.0f;    preset.params.bloom.tint = (gui_color_t){255, 255, 220, 255};    preset.params.bloom.anamorphic = GUI_FALSE;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_FALSE;    return preset;}// Preset shonen (estilo Naruto)anime_preset_t anime_preset_shonen(void){    anime_preset_t preset = {0};    preset.name = "Shonen Anime";    preset.style = ANIME_STYLE_SHONEN;    // Configurações de contorno    preset.params.outline.thickness = 2.5f;    preset.params.outline.edge_threshold = 0.3f;    preset.params.outline.color = (gui_color_t){20, 20, 20, 255};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_TRUE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 4;    preset.params.cel.smoothness = 0.15f;    preset.params.cel.specular = GUI_TRUE;    preset.params.cel.shade_threshold[0] = 0.2f;    preset.params.cel.shade_threshold[1] = 0.5f;    preset.params.cel.shade_threshold[2] = 0.7f;    preset.params.cel.shade_threshold[3] = 0.9f;    // Configurações de cor    preset.params.color.color_levels = 10;    preset.params.color.saturation = 1.4f;    preset.params.color.contrast = 1.3f;    preset.params.color.posterize = GUI_TRUE;    // Configurações de linhas de velocidade    preset.params.speed_lines.intensity = 0.8f;    preset.params.speed_lines.length = 50.0f;    preset.params.speed_lines.color = (gui_color_t){255, 255, 255, 200};    preset.params.speed_lines.density = 0.7f;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_TRUE;    return preset;}// Preset shojo (estilo Sailor Moon)anime_preset_t anime_preset_shojo(void){    anime_preset_t preset = {0};    preset.name = "Shojo Anime";    preset.style = ANIME_STYLE_SHOJO;    // Configurações de contorno    preset.params.outline.thickness = 1.8f;    preset.params.outline.edge_threshold = 0.35f;    preset.params.outline.color = (gui_color_t){40, 20, 40, 255};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_FALSE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 5;    preset.params.cel.smoothness = 0.3f;    preset.params.cel.specular = GUI_TRUE;    preset.params.cel.shade_threshold[0] = 0.15f;    preset.params.cel.shade_threshold[1] = 0.35f;    preset.params.cel.shade_threshold[2] = 0.55f;    preset.params.cel.shade_threshold[3] = 0.75f;    preset.params.cel.shade_threshold[4] = 0.95f;    // Configurações de cor    preset.params.color.color_levels = 16;    preset.params.color.saturation = 1.5f;    preset.params.color.contrast = 1.1f;    preset.params.color.posterize = GUI_TRUE;    // Configurações de bloom    preset.params.bloom.intensity = 1.0f;    preset.params.bloom.threshold = 0.6f;    preset.params.bloom.radius = 15.0f;    preset.params.bloom.tint = (gui_color_t){255, 200, 255, 255};    preset.params.bloom.anamorphic = GUI_TRUE;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_FALSE;    return preset;}// Preset seinen (estilo Berserk)anime_preset_t anime_preset_seinen(void){    anime_preset_t preset = {0};    preset.name = "Seinen Anime";    preset.style = ANIME_STYLE_SEINEN;    // Configurações de contorno    preset.params.outline.thickness = 3.0f;    preset.params.outline.edge_threshold = 0.25f;    preset.params.outline.color = (gui_color_t){0, 0, 0, 255};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_TRUE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 3;    preset.params.cel.smoothness = 0.1f;    preset.params.cel.specular = GUI_FALSE;    preset.params.cel.shade_threshold[0] = 0.3f;    preset.params.cel.shade_threshold[1] = 0.6f;    preset.params.cel.shade_threshold[2] = 0.9f;    // Configurações de cor    preset.params.color.color_levels = 6;    preset.params.color.saturation = 0.8f;    preset.params.color.contrast = 1.4f;    preset.params.color.posterize = GUI_TRUE;    // Configurações de tramas    preset.params.tone.pattern_type = 2; // Crosshatch    preset.params.tone.scale = 5.0f;    preset.params.tone.threshold = 0.5f;    preset.params.tone.rotation = 45.0f;    preset.params.tone.inverted = GUI_FALSE;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_TRUE;    return preset;}// Preset chibi (estilo super-deformed)anime_preset_t anime_preset_chibi(void){    anime_preset_t preset = {0};    preset.name = "Chibi Anime";    preset.style = ANIME_STYLE_CHIBI;    // Configurações de contorno    preset.params.outline.thickness = 2.2f;    preset.params.outline.edge_threshold = 0.3f;    preset.params.outline.color = (gui_color_t){40, 40, 40, 255};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_FALSE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 4;    preset.params.cel.smoothness = 0.25f;    preset.params.cel.specular = GUI_TRUE;    preset.params.cel.shade_threshold[0] = 0.2f;    preset.params.cel.shade_threshold[1] = 0.4f;    preset.params.cel.shade_threshold[2] = 0.6f;    preset.params.cel.shade_threshold[3] = 0.8f;    // Configurações de cor    preset.params.color.color_levels = 12;    preset.params.color.saturation = 1.6f;    preset.params.color.contrast = 1.1f;    preset.params.color.posterize = GUI_TRUE;    // Configurações de bloom    preset.params.bloom.intensity = 0.7f;    preset.params.bloom.threshold = 0.6f;    preset.params.bloom.radius = 8.0f;    preset.params.bloom.tint = (gui_color_t){255, 230, 200, 255};    preset.params.bloom.anamorphic = GUI_FALSE;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_FALSE;    return preset;}// Preset cyberpunk (estilo Ghost in the Shell)anime_preset_t anime_preset_cyberpunk(void){    anime_preset_t preset = {0};    preset.name = "Cyberpunk Anime";    preset.style = ANIME_STYLE_CYBERPUNK;    // Configurações de contorno    preset.params.outline.thickness = 2.0f;    preset.params.outline.edge_threshold = 0.28f;    preset.params.outline.color = (gui_color_t){0, 40, 60, 255};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_TRUE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 5;    preset.params.cel.smoothness = 0.15f;    preset.params.cel.specular = GUI_TRUE;    preset.params.cel.shade_threshold[0] = 0.15f;    preset.params.cel.shade_threshold[1] = 0.35f;    preset.params.cel.shade_threshold[2] = 0.55f;    preset.params.cel.shade_threshold[3] = 0.75f;    preset.params.cel.shade_threshold[4] = 0.95f;    // Configurações de cor    preset.params.color.color_levels = 10;    preset.params.color.saturation = 1.2f;    preset.params.color.contrast = 1.3f;    preset.params.color.posterize = GUI_TRUE;    // Configurações de bloom    preset.params.bloom.intensity = 0.9f;    preset.params.bloom.threshold = 0.5f;    preset.params.bloom.radius = 12.0f;    preset.params.bloom.tint = (gui_color_t){100, 200, 255, 255};    preset.params.bloom.anamorphic = GUI_TRUE;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_TRUE;    return preset;}// Preset aquarela (estilo Ghibli)anime_preset_t anime_preset_watercolor(void){    anime_preset_t preset = {0};    preset.name = "Watercolor Anime";    preset.style = ANIME_STYLE_WATERCOLOR;    // Configurações de contorno    preset.params.outline.thickness = 1.2f;    preset.params.outline.edge_threshold = 0.4f;    preset.params.outline.color = (gui_color_t){60, 40, 20, 200};    preset.params.outline.color_based = GUI_TRUE;    preset.params.outline.depth_based = GUI_FALSE;    // Configurações de cel-shading    preset.params.cel.shade_levels = 6;    preset.params.cel.smoothness = 0.4f;    preset.params.cel.specular = GUI_FALSE;    preset.params.cel.shade_threshold[0] = 0.1f;    preset.params.cel.shade_threshold[1] = 0.3f;    preset.params.cel.shade_threshold[2] = 0.5f;    preset.params.cel.shade_threshold[3] = 0.7f;    preset.params.cel.shade_threshold[4] = 0.9f;    // Configurações de cor    preset.params.color.color_levels = 20;    preset.params.color.saturation = 1.1f;    preset.params.color.contrast = 1.0f;    preset.params.color.posterize = GUI_FALSE;    // Configurações de bloom    preset.params.bloom.intensity = 0.4f;    preset.params.bloom.threshold = 0.7f;    preset.params.bloom.radius = 20.0f;    preset.params.bloom.tint = (gui_color_t){255, 250, 240, 255};    preset.params.bloom.anamorphic = GUI_FALSE;    preset.params.global_intensity = 1.0f;    preset.params.enable_3d = GUI_FALSE;    return preset;}