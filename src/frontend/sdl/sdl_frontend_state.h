#ifndef SDL_FRONTEND_STATE_H#define SDL_FRONTEND_STATE_H#include <SDL2/SDL.h>#include <SDL2/SDL_gamecontroller.h>#include <stdbool.h>#include <stdint.h>#include "frontend/common/frontend_config.h"#include "frontend/sdl/sdl_game_renderer.h"typedef struct{    // Configuração    emu_frontend_config_t config;    // Renderização    sdl_game_renderer_t renderer;    // Áudio    SDL_AudioDeviceID audio_device;    uint8_t audio_buffer[8192];    // Estado do jogo    bool running;    bool paused;    bool show_menu;    bool show_fps;    // Métricas    float fps;    uint32_t frames_since_last_fps;    uint32_t last_fps_update;    // Controles    SDL_GameController *gamepad;    uint8_t controller_states[4];} sdl_frontend_state_t;#endif // SDL_FRONTEND_STATE_H