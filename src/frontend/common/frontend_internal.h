/** * @file frontend_internal.h * @brief Definições internas do frontend */#ifndef EMU_FRONTEND_INTERNAL_H#define EMU_FRONTEND_INTERNAL_H#include <stdbool.h>#include <stdint.h>/** * @brief Estrutura interna do frontend */struct emu_frontend{    bool initialized;    uint32_t *framebuffer;    int32_t width;    int32_t height;    void *window_handle;    void *platform_data; // Dados específicos da plataforma (SDL, etc.)};#endif /* EMU_FRONTEND_INTERNAL_H */