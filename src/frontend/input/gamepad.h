#ifndef GAMEPAD_H#define GAMEPAD_H#include <SDL2/SDL.h>#include <stdbool.h>#include <stdint.h>#ifdef __cplusplusextern "C"{#endif    // Definições para botões NES    typedef enum    {        GAMEPAD_NES_A = 0,        GAMEPAD_NES_B = 1,        GAMEPAD_NES_SELECT = 2,        GAMEPAD_NES_START = 3,        GAMEPAD_NES_UP = 4,        GAMEPAD_NES_DOWN = 5,        GAMEPAD_NES_LEFT = 6,        GAMEPAD_NES_RIGHT = 7,        GAMEPAD_NES_BUTTON_COUNT    } gamepad_nes_button_t;    // Inicialização e finalização    bool gamepad_init(void);    void gamepad_shutdown(void);    // Detecção e gerenciamento de gamepads    void gamepad_detect_devices(void);    bool gamepad_process_event(SDL_Event *event);    // Consultas de estado    uint16_t gamepad_get_state(int player_index);    bool gamepad_is_connected(int player_index);    int gamepad_get_count(void);    const char *gamepad_get_name(int player_index);    // Efeitos    bool gamepad_rumble(int player_index, float intensity, uint32_t duration_ms);#ifdef __cplusplus}#endif#endif /* GAMEPAD_H */