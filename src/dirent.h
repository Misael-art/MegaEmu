/** * dirent.h - Definição de estruturas de diretório para Windows (compatibilidade com sistemas Unix) */#ifndef _DIRENT_H_#define _DIRENT_H_#include <windows.h>#include <stdio.h>#include <stdint.h>#ifdef __cplusplusextern "C"{#endif    /* Definição de off_t para Windows */    typedef long off_t;/* Constantes para d_type */#define DT_UNKNOWN 0#define DT_REG 8#define DT_DIR 4    struct dirent    {        long d_ino;            /* inode number (sempre 1 no Windows) */        off_t d_off;           /* offset to next structure (sempre 0) */        uint16_t d_reclen;     /* tamanho deste registro */        uint8_t d_type;        /* tipo de arquivo */        char d_name[MAX_PATH]; /* nome do arquivo */    };    typedef struct _DIR    {        struct dirent dirent;      /* diretório atual */        HANDLE hFind;              /* pesquisa de arquivo aberta */        WIN32_FIND_DATAA fileinfo; /* informações do arquivo */        uint8_t cached;            /* se a entrada já foi lida */        char *pattern;             /* padrão de pesquisa */    } DIR;    DIR *opendir(const char *dirname);    struct dirent *readdir(DIR *dirp);    int32_t closedir(DIR *dirp);    void rewinddir(DIR *dirp);#ifdef __cplusplus}#endif#endif /*_DIRENT_H_*/