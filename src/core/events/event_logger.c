#include "event_logger.h"#include "utils/enhanced_log.h"void event_logger_log_event(EmuNodeEvent* event, EventLogLevel level) {    if (!event) return;    const char* event_type_str = emu_event_type_to_string(event->type);        LOG_DEBUG(EMU_LOG_CAT_CORE,         "Event: %s, Timestamp: %u, Data Size: %zu",        event_type_str,        event->timestamp,        event->data_size    );    if (level >= EVENT_LOG_LEVEL_TRACE && event->data) {        char* data_hex = emu_hex_dump(event->data, event->data_size);        LOG_TRACE(EMU_LOG_CAT_CORE, "Event Data: %s", data_hex);        free(data_hex);    }}