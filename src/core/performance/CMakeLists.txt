cmake_minimum_required(VERSION 3.10)# Arquivos fonte do módulo de performanceset(PERFORMANCE_SOURCES    ${CMAKE_CURRENT_SOURCE_DIR}/performance.c)# Arquivos de cabeçalho do módulo de performanceset(PERFORMANCE_HEADERS    ${CMAKE_CURRENT_SOURCE_DIR}/performance.h)# Adiciona os arquivos à biblioteca principaltarget_sources(emu_core PRIVATE ${PERFORMANCE_SOURCES})# Instala os cabeçalhosinstall(FILES ${PERFORMANCE_HEADERS} DESTINATION include/mega_emu/core/performance)