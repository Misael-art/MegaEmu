#include <string.h>#include <stdio.h>#include <stdint.h>#include <stdlib.h>// Definição da enumeração para compatibilidadetypedef enum {    EMU_PLATFORM_UNKNOWN = 0,    EMU_PLATFORM_AUTO,    EMU_PLATFORM_MEGADRIVE,    EMU_PLATFORM_MASTERSYSTEM,    EMU_PLATFORM_NES} emu_platform_type_t;/** * @brief Detecta a plataforma com base nos dados da ROM *  * @param rom_data Dados da ROM * @param rom_size Tamanho da ROM * @return emu_platform_type_t Tipo de plataforma detectada ou EMU_PLATFORM_UNKNOWN se não foi possível detectar */emu_platform_type_t detect_platform(const uint8_t* rom_data, size_t rom_size) {    if (!rom_data || rom_size == 0) {        printf("Platform Detector: ROM inválida (nula ou tamanho zero)\n");        return EMU_PLATFORM_UNKNOWN;    }        printf("Platform Detector: Analisando ROM de %zu bytes\n", rom_size);        // Para fins de teste, sempre retornar Mega Drive    printf("Platform Detector: Retornando Mega Drive para fins de teste\n");    return EMU_PLATFORM_MEGADRIVE;} 