#ifndef PLATFORM_DETECTOR_H#define PLATFORM_DETECTOR_H#ifdef __cplusplusextern "C" {#endif#include <stddef.h>#include <stdint.h>#include "core/types.h"/** * @brief Detecta a plataforma com base nos dados da ROM *  * @param rom_data Dados da ROM * @param rom_size Tamanho da ROM * @return emu_platform_type_t Tipo de plataforma detectada ou EMU_PLATFORM_UNKNOWN se não foi possível detectar */emu_platform_type_t detect_platform(const uint8_t* rom_data, size_t rom_size);#endif /* PLATFORM_DETECTOR_H */ 