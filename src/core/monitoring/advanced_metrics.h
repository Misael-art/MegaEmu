#include <stdint.h>typedef struct {    MetricID id;    MetricType type;    double value;    TimeStamp timestamp;    MetricMetadata metadata;} AdvancedMetric;typedef struct {    AdvancedMetric** metrics;    size_t metric_count;        void (*collect_metric)(MetricType type);    void (*analyze_trend)(MetricID id);    void (*generate_alert)(AlertLevel level);    MetricReport* (*create_report)(void);} MetricsManager;typedef struct {    MemoryMetrics* memory;    CPUMetrics* cpu;    NetworkMetrics* network;    SystemMetrics* system;        void (*start_monitoring)(void);    void (*collect_all_metrics)(void);    void (*export_metrics)(ExportFormat format);} SystemMonitor;