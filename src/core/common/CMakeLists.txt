# CMakeLists.txt para o diretório common# Coletar arquivos fontefile(GLOB_RECURSE COMMON_SOURCES "*.c")file(GLOB_RECURSE COMMON_HEADERS "*.h")# Adicionar arquivos à biblioteca emu_coretarget_sources(emu_core    PRIVATE        ${COMMON_SOURCES}    PUBLIC        ${COMMON_HEADERS})# Adicionar diretório atual como include pathtarget_include_directories(emu_core    PUBLIC        ${CMAKE_CURRENT_SOURCE_DIR})