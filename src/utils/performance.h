#ifndef PERFORMANCE_H#define PERFORMANCE_H/** * @file performance.h * @brief Macros e funções para medição de desempenho */#include <stdint.h>// Macros para perfil de desempenho#ifdef EMU_ENABLE_PROFILING// Implementação real das macros de perfilvoid emu_profile_function_start(const char *function_name);void emu_profile_function_end(const char *function_name);void emu_profile_section_start(const char *section_name);void emu_profile_section_end(const char *section_name);#define EMU_PROFILE_FUNCTION() emu_profile_function_start(__FUNCTION__)#define EMU_PROFILE_SECTION(name) emu_profile_section_start(name)#else// Versões vazias das macros quando o perfil está desativado#define EMU_PROFILE_FUNCTION()#define EMU_PROFILE_SECTION(name)#endif#endif // PERFORMANCE_H