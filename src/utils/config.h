#include <stdint.h>/** * @file config.h * @brief Configurações globais para o emulador Mega_Emu */#ifndef EMU_CONFIG_H#define EMU_CONFIG_H#ifdef __cplusplusextern "C" {#endif/* Configurações de compilação */#define EMU_DISABLE_ERROR_HANDLING 1  /* Desativa o sistema de tratamento de erros */#define EMU_DISABLE_LOGGING 1         /* Desativa o sistema de log *//* Configurações de plataforma */#if defined(_WIN32) || defined(_WIN64)    #define EMU_PLATFORM_WINDOWS 1    #define EMU_PLATFORM_LINUX 0    #define EMU_PLATFORM_MACOS 0#elif defined(__linux__)    #define EMU_PLATFORM_WINDOWS 0    #define EMU_PLATFORM_LINUX 1    #define EMU_PLATFORM_MACOS 0#elif defined(__APPLE__)    #define EMU_PLATFORM_WINDOWS 0    #define EMU_PLATFORM_LINUX 0    #define EMU_PLATFORM_MACOS 1#else    #error "Plataforma não suportada"#endif/* Configurações de compilador */#if defined(_MSC_VER)    #define EMU_COMPILER_MSVC 1    #define EMU_COMPILER_GCC 0    #define EMU_COMPILER_CLANG 0    /* Desativa avisos para funções inseguras no MSVC */    #define _CRT_SECURE_NO_WARNINGS#elif defined(__GNUC__)    #define EMU_COMPILER_MSVC 0    #define EMU_COMPILER_GCC 1    #define EMU_COMPILER_CLANG 0#elif defined(__clang__)    #define EMU_COMPILER_MSVC 0    #define EMU_COMPILER_GCC 0    #define EMU_COMPILER_CLANG 1#else    #error "Compilador não suportado"#endif/* Configurações de depuração */#if !defined(NDEBUG) || defined(_DEBUG)    #define EMU_DEBUG 1#else    #define EMU_DEBUG 0#endif#endif /* EMU_CONFIG_H */