# CMakeLists.txt para o componente de input do NES# Definir arquivos fonteset(NES_INPUT_SOURCES    nes_input.c)# Definir arquivos de cabeçalhoset(NES_INPUT_HEADERS    nes_input.h)# Adicionar arquivos à biblioteca emu_nestarget_sources(emu_nes    PRIVATE        ${NES_INPUT_SOURCES}    PUBLIC        ${NES_INPUT_HEADERS})# Adicionar diretório atual como include pathtarget_include_directories(emu_nes    PUBLIC        ${CMAKE_CURRENT_SOURCE_DIR})