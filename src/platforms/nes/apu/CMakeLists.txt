# CMakeLists.txt para o componente de APU do NES# Definir arquivos fonte Cset(NES_APU_C_SOURCES    nes_apu.c)# Definir arquivos fonte C++set(NES_APU_CPP_SOURCES    nes_apu.cpp)# Definir arquivos de cabeçalhoset(NES_APU_HEADERS    nes_apu.h    nes_apu.hpp)# Adicionar arquivos à biblioteca mega_emu_nestarget_sources(mega_emu_nes    PRIVATE        ${NES_APU_C_SOURCES}        ${NES_APU_CPP_SOURCES}    PUBLIC        ${NES_APU_HEADERS})# Adicionar diretório atual como include pathtarget_include_directories(mega_emu_nes    PUBLIC        ${CMAKE_CURRENT_SOURCE_DIR})# Definir flags específicas para compilação C++set_source_files_properties(    ${NES_APU_CPP_SOURCES}    PROPERTIES    COMPILE_FLAGS "${CXX_STANDARD_FLAGS}")