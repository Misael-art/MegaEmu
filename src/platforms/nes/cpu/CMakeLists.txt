# CMakeLists.txt para o componente de CPU do NES (RP2A03)# Definir arquivos fonte Cset(NES_CPU_C_SOURCES    nes_cpu.c    rp2a03.c)# Definir arquivos fonte C++set(NES_CPU_CPP_SOURCES    nes_cpu.cpp)# Definir arquivos de cabeçalhoset(NES_CPU_HEADERS    nes_cpu.h    nes_cpu.hpp    rp2a03.h)# Adicionar arquivos à biblioteca mega_emu_nestarget_sources(mega_emu_nes    PRIVATE        ${NES_CPU_C_SOURCES}        ${NES_CPU_CPP_SOURCES}    PUBLIC        ${NES_CPU_HEADERS})# Adicionar diretório atual como include pathtarget_include_directories(mega_emu_nes    PUBLIC        ${CMAKE_CURRENT_SOURCE_DIR})# Definir flags específicas para compilação C++set_source_files_properties(    ${NES_CPU_CPP_SOURCES}    PROPERTIES    COMPILE_FLAGS "${CXX_STANDARD_FLAGS}")