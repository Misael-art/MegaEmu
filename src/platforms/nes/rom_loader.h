#include <stdint.h>typedef struct {    uint8_t* prg_rom;    uint8_t* chr_rom;    uint32_t prg_size;    uint32_t chr_size;    uint8_t mapper_id;    bool has_battery;    bool mirroring;} NESRomData;typedef struct {    char signature[4];    // "NES^Z"    uint8_t prg_banks;    // Número de bancos PRG ROM de 16KB    uint8_t chr_banks;    // Número de bancos CHR ROM de 8KB    uint8_t flags6;       // Flags de mapeamento    uint8_t flags7;       // Mais flags    uint8_t reserved[8];  // Deve ser zero} NESHeader;// Interface de carregamentobool nes_load_rom(const char* path, NESRomData* rom_data);void nes_unload_rom(NESRomData* rom_data);bool nes_validate_rom(const NESRomData* rom_data);