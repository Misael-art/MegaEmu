cmake_minimum_required(VERSION 3.10)# Biblioteca PPUset(PPU_SOURCE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/src)set(PPU_INCLUDE_DIR ${CMAKE_CURRENT_SOURCE_DIR}/src)set(PPU_SOURCES    ${PPU_SOURCE_DIR}/nes_ppu.cpp)set(PPU_HEADERS    ${PPU_INCLUDE_DIR}/nes_ppu.hpp)add_library(nes_ppu STATIC    ${PPU_SOURCES}    ${PPU_HEADERS})target_include_directories(nes_ppu PUBLIC    ${PPU_INCLUDE_DIR})# Configurações de compilaçãoset_target_properties(nes_ppu PROPERTIES    CXX_STANDARD 17    CXX_STANDARD_REQUIRED ON)# Testesif(BUILD_TESTS)    add_subdirectory(tests)endif()# Exporta a bibliotecaexport(TARGETS nes_ppu FILE NESPPUConfig.cmake)