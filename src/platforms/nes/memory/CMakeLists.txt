# CMakeLists.txt para o componente de memória do NES# Definir arquivos fonteset(NES_MEMORY_SOURCES    nes_memory.c)# Definir arquivos de cabeçalhoset(NES_MEMORY_HEADERS    nes_memory.h)# Adicionar arquivos à biblioteca emu_nestarget_sources(emu_nes    PRIVATE        ${NES_MEMORY_SOURCES}    PUBLIC        ${NES_MEMORY_HEADERS})# Adicionar diretório atual como include pathtarget_include_directories(emu_nes    PUBLIC        ${CMAKE_CURRENT_SOURCE_DIR})