#include <stdint.h>typedef enum {    EMU_DMA_VRAM_FILL,    EMU_DMA_VRAM_COPY,    EMU_DMA_VRAM_68K} emu_dma_mode_t;typedef struct {    uint32_t source_addr;    uint16_t length;    uint16_t destination;    emu_dma_mode_t mode;    void (*dma_complete_callback)(void);} emu_vdp_dma_t;// Funções públicasvoid emu_vdp_dma_init(emu_vdp_dma_t* dma);bool emu_vdp_dma_process(emu_vdp_dma_t* dma, int32_t cycles);void emu_vdp_dma_start_transfer(emu_vdp_dma_t* dma, emu_dma_mode_t mode);