# Testes da CPU do MegaDriveset(TEST_MEGADRIVE_CPU_SOURCES    test_m68000cpu.cpp)# Adicionar executável de testeadd_executable(test_m68000cpu    ${TEST_MEGADRIVE_CPU_SOURCES})# Incluir diretórios necessáriostarget_include_directories(test_m68000cpu PRIVATE    ${CMAKE_SOURCE_DIR}/src)# Linkar com bibliotecas necessáriastarget_link_libraries(test_m68000cpu    megadrive_cpu    utils)# Definir flags de compilaçãotarget_compile_features(test_m68000cpu PRIVATE cxx_std_17)# Adicionar testeadd_test(    NAME test_m68000cpu    COMMAND $<TARGET_FILE:test_m68000cpu>    WORKING_DIRECTORY ${CMAKE_BINARY_DIR})# Adicionar alvo para executar o testeadd_custom_target(run_test_m68000cpu    COMMAND $<TARGET_FILE:test_m68000cpu>    DEPENDS test_m68000cpu    WORKING_DIRECTORY ${CMAKE_BINARY_DIR}    COMMENT "Executando testes de CPU do MegaDrive")